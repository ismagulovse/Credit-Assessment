<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Порог обязательных лаб
    |--------------------------------------------------------------------------
    | Сколько лаб студент должен сдать, чтобы быть допущенным к экзамену.
    | Сдал >= этого числа — допуск. Меньше — не допущен.
    | Значение берётся из .env, без хардкода.
    */
    'required_labs' => (int) env('REQUIRED_LABS', 4),

    /*
    |--------------------------------------------------------------------------
    | Максимальный размер загружаемого файла (МБ)
    |--------------------------------------------------------------------------
    */
    'upload_max_size_mb' => (int) env('UPLOAD_MAX_SIZE_MB', 10),

    /*
    |--------------------------------------------------------------------------
    | Имя листа с данными
    |--------------------------------------------------------------------------
    | Если null — берётся первый лист книги.
    */
    'sheet_name' => env('ATTENDANCE_SHEET_NAME', null),

];
